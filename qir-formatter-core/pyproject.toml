[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "qir-formatter-core"
requires-python = ">=3.10"
dynamic = ["version"]

[tool.maturin]
module-name = "qir_formatter_core"
features = ["pyo3/extension-module"]
python-source = "python"
